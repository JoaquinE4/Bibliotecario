<main class="container my-5">

    <div class="d-flex justify-content-between align-items-center border-bottom pb-3 mb-4">
        <h1 class="display-5 fw-bold text-dark mb-0">Escritores</h1>

        <?php if (isset($this->user->rol) && $this->user->rol === 'admin'): ?>
        <a href="escritor/nuevo" class="btn btn-primary d-flex align-items-center gap-1">
            <span class="fs-5 fw-bold leading-none">+</span> Nuevo Escritor
        </a>
        <?php endif; ?>
    </div>
    <?php require_once  __DIR__ . '/../layout/filter.view.phtml'  ?>
    <?php if (empty($escritores)): ?>
    <div class="alert alert-info text-center py-4 shadow-sm" role="alert">
        <p class="mb-0 fw-medium">No hay escritores registrados.</p>
    </div>

    <?php else: ?>
    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
        <?php foreach ($escritores as $escritor): ?>
        <div class="col">
            <div class="card h-100 shadow-sm border-0 bg-white">


                <div class="card-body d-flex flex-column">
                    <h2 class="card-title h4 text-dark fw-semibold mb-2"><?= htmlspecialchars($escritor->nombre) ?></h2>


                    <div class="mb-3 small text-muted">
                        <span class="badge bg-light text-dark border me-1">📍
                            <?= htmlspecialchars($escritor->origen) ?></span>
                        <span class="badge bg-light text-dark border">📅
                            <?= date('d/m/Y', strtotime($escritor->fecha_nac)) ?></span>
                    </div>


                    <p class="card-text text-secondary text-truncate-3 flex-grow-1">
                        <?= htmlspecialchars(substr($escritor->descripcion ?? '', 0, 150)) ?>...
                    </p>


                    <div class="mt-3">
                        <a href="escritor/<?= $escritor->id ?>" class="btn btn-outline-primary btn-sm w-100">Ver más</a>
                    </div>
                </div>
            </div>
        </div>
        <?php endforeach; ?>
    </div>
    <?php endif; ?>
</main>