<main class="container my-5">
    <div class="d-flex justify-content-between align-items-center border-bottom pb-3 mb-4">
        <h1 class="display-5 fw-bold text-dark mb-0">Libros</h1>

        <?php if (isset($this->user->rol) && $this->user->rol === 'admin'): ?>
            <a href="libro/nuevo" class="btn btn-primary d-flex align-items-center gap-1">
                <span class="fs-5 fw-bold leading-none">+</span> Nuevo Libro
            </a>
        <?php endif; ?>
    </div>

    <?php if (empty($libros)): ?>
        <div class="alert alert-info text-center py-4 shadow-sm" role="alert">
            <p class="mb-0 fw-medium">No hay libros registrados.</p>
        </div>

    <?php else: ?>
        <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
            <?php foreach ($libros as $libro): ?>
                <div class="col">
                    <div class="card h-100 shadow-xl border-0 bg-white">

                        <div class="card-body d-flex flex-column">
                            <h2 class="card-title h4 text-dark fw-semibold mb-1"><?= htmlspecialchars($libro->titulo) ?></h2>

                            <p class="text-muted small mb-3">
                                Por <span
                                    class="text-primary fw-medium"><?= htmlspecialchars($libro->autor_nombre ?? 'Desconocido') ?></span>
                                <span class="mx-1">&bull;</span> Año <?= $libro->anio ?>
                            </p>

                            <p class="card-text text-secondary flex-grow-1 text-truncate-3">
                                <?= htmlspecialchars(substr($libro->sinopsis ?? '', 0, 150)) ?>...
                            </p>

                            <div class="mt-3">
                                <a href="libro/<?= $libro->id ?>" class="btn btn-outline-primary btn-sm w-100">Ver más</a>
                            </div>
                        </div>



                    </div>
                </div>
            <?php endforeach; ?>
        </div>
    <?php endif; ?>
</main>